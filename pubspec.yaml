name: ind_utils
description: 'Ind_utils package to simplify SizedBox and Padding usage, reducing repetitive code.'
version: 1.0.3
homepage: https://github.com/ansaralisah/ind_utils.git
license: MIT

environment:
  sdk: '>=3.0.6 <4.0.0'
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter

  path: ^1.8.0
  interact: ^2.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0